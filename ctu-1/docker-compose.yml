version: "3"

services:
  geth:
    image: ethereum/client-go
    restart: "no"
    ports:
      - "30301:30301"
      - "30301:30301/udp"
      - "8891:8891"
      - "8892:8892"
    volumes:
      - /eth:/eth
    stop_signal: SIGINT
    stop_grace_period: 30m
    command:
      - --port=30301
      - --http
      - --http.api
      - "eth,net,web3"
      - --http.addr=0.0.0.0
      - --http.vhosts=*
      - --http.port=8891
      - --http.corsdomain=*
      - --rpc.gascap=15000000000000000
      - --ws
      - --ws.origins=*
      - --ws.addr=0.0.0.0
      - --ws.port=8892
      - --ws.api
      - "eth,net,web3,engine"
      - --datadir
      - "/eth/master"
      # - --bootnodes
      # - "enode://b93f61aab106c112942f6b3da1a98cad983cdb4a769c6a179fe5a5b84b98b2ff8a87721b476ec002d9a3b8dbc49d09e44bcc8c417005e16eabc9485f3b708817@192.168.1.204:30304,node://6b545d268bb671011eeeece346647dc53036a51110d3dc61b61e00c2393bda595294ac66e2eb6f7f0483998cf4712c51afa641680318a48a03b4a4fd2e85c80d@127.0.0.1:30302"
      # Minimum number of executable transaction slots guaranteed per account (default: 16)
      # - --txpool.accountslots=16
      # Maximum number of non-executable transaction slots for all accounts (default: 1024)
      # --txpool.globalqueue=1024
      # Maximum number of executable transaction slots for all accounts (default: 4096)
      # - --txpool.globalslots=10000
      # Maximum amount of time non-executable transaction are queued (default: 3h0m0s)
      # - --txpool.lifetime=3h0m0s
      # Megabytes of memory allocated to internal caching (default: 1024)
      - --cache=65536
      # Maximum number of network peers (network disabled if set to 0) (default: 25)
      - --maxpeers=150
      # Blockchain sync mode ("fast", "full", or "light")
      - --syncmode=snap
      # Blockchain garbage collection mode ("full", "archive") (default: "full")
      # - --gcmode=full
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "10"
  prysm:
    image: gcr.io/prysmaticlabs/prysm/beacon-chain:stable
    restart: "no"
    ports:
      - "8545:8545"
      - "3500:3500"
      - "4000:4000"
      - "8080:8080"
      - "13001:13001"
      - "12001:12001/udp"
    stop_signal: SIGINT
    stop_grace_period: 30m
    depends_on:
      - geth
    volumes:
      - /eth:/eth
    command: [
      "--p2p-host-ip=118.113.189.47",
      "--rpc-host=0.0.0.0",
      "--grpc-gateway-host=0.0.0.0",
      "--grpc-gateway-port=3500",
      "--grpc-gateway-corsdomain=http://node-status.mevs.cc:8888,http://localhost:4200,http://localhost:7500,http://127.0.0.1:4200,http://127.0.0.1:7500,http://0.0.0.0:4200,http://0.0.0.0:7500,http://localhost:3000,http://0.0.0.0:3000,http://127.0.0.1:3000",
      "--monitoring-host=0.0.0.0",
      "--p2p-tcp-port=13001",
      "--p2p-udp-port=12001",
      "--accept-terms-of-use",
      "--execution-endpoint=/eth/master/geth.ipc",
      "--datadir=/eth/cs/master"
    ]
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "10"